name: Java CI for CollinearPoints

on:
  push:
    paths:
      - 'Lecture6/collinear/**'
  pull_request:
    paths:
      - 'Lecture6/collinear/**'
  workflow_dispatch: # Add this line

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Compile Java Code
        run: javac -cp .:lib/* Lecture6/collinear/*.java

      - name: Run Tests
        run: java -cp .:lib/* org.junit.runner.JUnitCore Lecture6.collinear.TestCollinear
